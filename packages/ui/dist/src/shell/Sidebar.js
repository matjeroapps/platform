import React from 'react';
import { Badge } from '../components/Badge.js';
export const Sidebar = ({ appTitle = 'MatjerHub', appLogo, navItems, currentPath = '/', onNavigate, collapsed = false, onToggleCollapse, }) => {
    return (React.createElement("aside", { style: {
            width: collapsed ? '64px' : '240px',
            height: '100vh',
            backgroundColor: 'var(--color-card)',
            borderRight: '1px solid var(--color-border)',
            display: 'flex',
            flexDirection: 'column',
            transition: 'width var(--transition-normal)',
            zIndex: 800,
            position: 'sticky',
            top: 0,
        } },
        React.createElement("div", { style: {
                height: '60px',
                padding: '0 16px',
                display: 'flex',
                alignItems: 'center',
                justifyContent: collapsed ? 'center' : 'space-between',
                borderBottom: '1px solid var(--color-border)',
            } },
            React.createElement("div", { style: { display: 'flex', alignItems: 'center', gap: '10px' } },
                appLogo || (React.createElement("div", { style: {
                        width: '28px',
                        height: '28px',
                        borderRadius: 'var(--radius-md)',
                        backgroundColor: 'var(--color-primary)',
                        color: 'var(--color-on-primary)',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontWeight: 700,
                        fontSize: '14px',
                    } }, "M")),
                !collapsed && (React.createElement("span", { style: { fontWeight: 700, fontSize: '16px', color: 'var(--color-foreground)' } }, appTitle))),
            onToggleCollapse && !collapsed && (React.createElement("button", { onClick: onToggleCollapse, style: {
                    background: 'none',
                    border: 'none',
                    color: 'var(--color-muted-foreground)',
                    cursor: 'pointer',
                    fontSize: '14px',
                } }, "\u25C0"))),
        React.createElement("nav", { style: { flex: 1, padding: '12px 8px', overflowY: 'auto' } },
            React.createElement("ul", { style: { listStyle: 'none', margin: 0, padding: 0, display: 'flex', flexDirection: 'column', gap: '4px' } }, navItems.map((item) => {
                const isActive = currentPath === item.path || currentPath.startsWith(item.path + '/');
                return (React.createElement("li", { key: item.id },
                    React.createElement("button", { onClick: () => onNavigate && onNavigate(item.path), style: {
                            width: '100%',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: collapsed ? 'center' : 'space-between',
                            gap: '10px',
                            padding: '10px 12px',
                            borderRadius: 'var(--radius-md)',
                            backgroundColor: isActive ? 'var(--color-primary)' : 'transparent',
                            color: isActive ? 'var(--color-on-primary)' : 'var(--color-foreground)',
                            border: 'none',
                            fontWeight: isActive ? 600 : 400,
                            fontSize: '14px',
                            cursor: 'pointer',
                            transition: 'var(--transition-fast)',
                            textAlign: 'left',
                        } },
                        React.createElement("div", { style: { display: 'flex', alignItems: 'center', gap: '10px' } },
                            item.icon && React.createElement("span", null, item.icon),
                            !collapsed && React.createElement("span", null, item.label)),
                        !collapsed && item.badge && (React.createElement(Badge, { size: "sm", variant: isActive ? 'secondary' : 'default' }, item.badge)))));
            }))),
        onToggleCollapse && collapsed && (React.createElement("div", { style: { padding: '12px', borderTop: '1px solid var(--color-border)', textAlign: 'center' } },
            React.createElement("button", { onClick: onToggleCollapse, style: {
                    background: 'none',
                    border: 'none',
                    color: 'var(--color-muted-foreground)',
                    cursor: 'pointer',
                    fontSize: '14px',
                } }, "\u25B6")))));
};
